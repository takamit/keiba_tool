以下の内容を前提に、このプロジェクトの修正・追加・改善を行ってください。

【プロジェクト名】
競馬予想ツール

【開発フェーズ】
実用プロトタイプ（完成扱い・安定運用フェーズ）
※大規模変更は禁止、改善・拡張のみ許可

━━━━━━━━━━━━━━━━━━━━━━━
【最重要ルール（絶対遵守）】
━━━━━━━━━━━━━━━━━━━━━━━
- 既存の正常動作を絶対に壊さないこと
- 勝手な設計変更は禁止
- 部分修正は禁止 → 必ず整合性のある完全版を出すこと
- クラス名 / 関数名 / import を変更しないこと
- ディレクトリ構成を変更しないこと
- GUI / データ取得 / 学習 / 予測 の機能は必ず維持
- 不明点があっても現行構成を優先
- 修正時は必ず「どのファイルを置換するか」を明示
- 原則「丸ごと置換」で出力
- 未完成コードは禁止
- UI改善済み要素を絶対に巻き戻さないこと

━━━━━━━━━━━━━━━━━━━━━━━
【固定ディレクトリ構成（変更禁止）】
━━━━━━━━━━━━━━━━━━━━━━━
keiba_tool/
├─ main.py
├─ config.py
├─ requirements.txt
├─ core/
│  ├─ collector.py
│  ├─ parser.py
│  └─ dataset.py
├─ ml/
│  ├─ trainer.py
│  └─ predictor.py
├─ ui/
│  └─ gui.py
├─ data/
├─ models/
├─ cache/
└─ logs/

━━━━━━━━━━━━━━━━━━━━━━━
【固定import契約（変更禁止）】
━━━━━━━━━━━━━━━━━━━━━━━
- main.py → from ui.gui import AppGUI
- gui.py → class AppGUI(tk.Tk)
- collector.py → get_race_ids, fetch_race_page
- parser.py → parse_entry, parse_result
- dataset.py → FEATURE_COLUMNS, TARGET_COLUMNS, build系, enrich系, prepare_train_xy
- trainer.py → train_all_models
- predictor.py → predict_from_entry

━━━━━━━━━━━━━━━━━━━━━━━
【目的】
━━━━━━━━━━━━━━━━━━━━━━━
netkeibaからレース情報を取得し、
学習 → 複数モデル生成 → 予測まで行うGUIツール

━━━━━━━━━━━━━━━━━━━━━━━
【GUI仕様（固定）】
━━━━━━━━━━━━━━━━━━━━━━━
タブは3つ固定：
- データ取得
- AI学習
- 予測

━━━━━━━━━━━━━━━━━━━━━━━
【現在のGUI仕様（重要）】
━━━━━━━━━━━━━━━━━━━━━━━
- カレンダーで日付選択（単日 / 期間）
- 年移動 / 月移動は分離済み
- 日付は手動入力可能
- フォルダ選択は参照ボタンあり
- フルパス表示
- ログは読み取り専用
- ログ欄は視認性改善済み
- ログ欄に縦・横スクロールあり
- マウスホイール / Shift+ホイール対応
- AI学習タブ全体にスクロールあり
- 予測タブ全体にスクロールあり
- プログレスバーあり（％＋残り時間）
- 一時停止 / 停止あり
- メニューバーあり
- プロパティ画面あり
  - HTMLキャッシュ ON/OFF（デフォルトOFF）
  - キャッシュ削除機能あり

━━━━━━━━━━━━━━━━━━━━━━━
【UIデザインルール（厳守）】
━━━━━━━━━━━━━━━━━━━━━━━
- チェックボックスは必ず「☑ / ☐」で表示
- 「×」表示は禁止
- カレンダーの矢印・操作UIは統一感を維持
- ログは埋もれない構造を維持
- スクロール機能は削除禁止
- 視認性を最優先
- 情報は「一目で理解できる形」で表示

━━━━━━━━━━━━━━━━━━━━━━━
【予測機能仕様】
━━━━━━━━━━━━━━━━━━━━━━━
- モデル選択可能
- デフォルトは auto_best
- 競馬場指定オプションあり
- ラウンド指定（例：1R〜12R）あり
- 買い目形式対応：
  - 単勝
  - 複勝
  - 馬連ボックス
  - ワイドボックス
  - 三連複ボックス
  - 三連単フォーメーション
- 予測結果は
  - ログ出力
  - 視覚的に分かりやすい表示（表・強調）
  の両方で出すこと
- pred_rank / score / popularity_diff の可視性維持
- CSV出力も見やすさを考慮

━━━━━━━━━━━━━━━━━━━━━━━
【学習仕様】
━━━━━━━━━━━━━━━━━━━━━━━
- Seleniumでrace_id取得（変更禁止）
- requestsで詳細取得
- HTMLキャッシュはオプション制御
- 特徴量は削除禁止（欠損対応で処理）
- training_summary.json を出力
- GUIで学習結果を確認可能
- モデルは複数前提：
  - 競馬場特化
  - 天候特化
  - 距離特化
  - 馬・騎手特化
  - 調教師特化
  - 総合モデル
- モデル選択機能を維持
- デフォルトは最適モデル（auto_best）
- 自己学習は安全設計で維持

━━━━━━━━━━━━━━━━━━━━━━━
【特徴量（削除禁止）】
━━━━━━━━━━━━━━━━━━━━━━━
track_code
weather_code
ground_code
surface_code
direction_code
race_no
month
day
weekday
distance
frame_no
horse_no
age
carried_weight
body_weight
body_weight_diff
odds
odds_log
popularity
popularity_rev
sex_code
field_size
frame_ratio
horse_no_ratio
is_favorite
is_outer_half
is_grade
is_newcomer
is_maiden

━━━━━━━━━━━━━━━━━━━━━━━
【ターゲット】
━━━━━━━━━━━━━━━━━━━━━━━
target_win
target_top2
target_top3
target_top5
target_favorite_win
target_longshot_top3

━━━━━━━━━━━━━━━━━━━━━━━
【改善方針】
━━━━━━━━━━━━━━━━━━━━━━━
- dataset精度向上
- クラス不均衡対応
- f1=0問題改善
- スコア合成最適化
- 回収率ベース評価
- モデル比較可視化
- UI/UX改善
- ログ視認性強化

━━━━━━━━━━━━━━━━━━━━━━━
【禁止事項】
━━━━━━━━━━━━━━━━━━━━━━━
- 「良さそうだから」で設計変更
- Selenium廃止
- requestsへの全面置換
- GUI削除・簡略化
- モデル単一化
- 特徴量削除
- UI改善の巻き戻し
- チェックボックスを×に戻す
- ログが見えない構造への変更

━━━━━━━━━━━━━━━━━━━━━━━
【出力ルール】
━━━━━━━━━━━━━━━━━━━━━━━
- 修正対象ファイルを必ず明示
- 原則「丸ごと置換」
- コピペ可能形式
- 未完成コード禁止
- 必ず動作を考慮
- Gitコマンド提示：
  git add
  git commit
  git push

━━━━━━━━━━━━━━━━━━━━━━━
【実行環境】
━━━━━━━━━━━━━━━━━━━━━━━
- venvから起動
- python main.py
- PowerShell前提で案内OK

━━━━━━━━━━━━━━━━━━━━━━━
【重要補足】
━━━━━━━━━━━━━━━━━━━━━━━
このツールは既に完成状態に近いため、
修正は「改善・拡張」に限定すること。

破壊的変更は禁止。


競馬予想ツールの既存構成・import契約・GUI仕様・UI改善（カレンダー/ログ/スクロール/☑チェック）を絶対に壊さずに修正してください。
部分修正は禁止、整合性の取れた完全版を出してください。
修正対象ファイルを明示し、丸ごと置換でお願いします。